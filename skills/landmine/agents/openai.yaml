interface:
  display_name: "Landmine"
  short_description: "Find local Git-backed code-change risks before editing."
  default_prompt: "Use $landmine to analyze this requested code change, separate evidence from inference, and preserve all alpha limitations."
